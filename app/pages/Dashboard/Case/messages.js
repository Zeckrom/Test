/*
 * Case Messages
 *
 * This contains all the text for the Case component.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.components.Case'

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Case component!',
  },
})
